<h1><%= link_to @song.title, song_path(@song) %></h1>
<% if @song.artist %>
  <p><%= display_artist(@song) %></p>
<% else %>
  <%= link_to "Add Artist", edit_song_path(@song) %>
<% end %>
<%= link_to "All Songs", songs_path %>
